version: 1.0
runtime: Nodejs12
build:
  commands:
    pre-build:
      - cd ./frontend
    build:        
      - npm install
    post-build:
      - cd ../
run:
  command: cd ./frontend && npm run dev
  network: 
    port: 3000
